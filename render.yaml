
services:
  - type: web
    name: blue-link-frontend
    env: static
    buildCommand: npm run build
    staticPublishPath: build
    routes:
      # 👇 Fix React Router "Not Found" issue
      - type: rewrite
        source: /*
        destination: /index.html
